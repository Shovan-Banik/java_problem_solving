function findArea( length, width ) {
    //Write your code here
    //Don't forget to return
    var area=length*width;
    return area;
    
    }
    var result=findArea(10,5);
    console.log(result);